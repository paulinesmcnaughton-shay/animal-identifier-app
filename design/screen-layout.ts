import { space } from '@/design/tokens'

/** Horizontal inset and header metrics — match Settings screen. */
export const screenLayout = {
  padH: space[16],
  headerPadV: space[12],
  iconBtnSize: 40,
  iconSize: 24,
  iconPressedOpacity: 0.6,
} as const
